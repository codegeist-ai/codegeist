codegeist:
  provider:
    ollama:
      type: ollama
      name: Ollama
      base-url: http://localhost:11434
    openai:
      type: openai
      name: OpenAI
      api-key: test-openai-secret
      organization-id: org-test
