# Legacy Chat Memory

The canonical project memory is `docs/memory-bank/chat.md`.

This legacy root-level file is kept only as a pointer for older sessions that
still look for `chat.md` in the repository root. Do not add new project state
here; update `docs/memory-bank/chat.md` instead.
