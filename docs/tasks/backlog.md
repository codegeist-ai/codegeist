# Task Backlog

Quick-capture list for task ideas that are not active task context yet. Each
entry is a single idea line and can later be expanded into a tracked task under
`docs/tasks/`.

* Import and analyze https://github.com/Hmbown/DeepSeek-TUI and evaluate what can be adopted.
* Implement JBang extension runtime after the T003 core tool, permission, workspace, shell, and native-readiness gates are stable.
* Implement PF4J packaged plugin runtime after the T003 core runtime, tool registry, permission model, workspace policy, and native-readiness gates are stable.
* Implement Vaadin client after T003 runtime/session/event projections and approval semantics are stable.
* Continue `T008_build-codegeist-cloud-server` with child tasks for auth, S3 artifact storage, OpenRouter proxy, and CLI sync.
* Implement API and SDK/OpenAPI surface after T008 cloud API contracts are stable enough to version.
