# yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
# envoy-ai-gateway-smoke.yaml - authentik bootstrap for the Envoy AI Gateway smoke.
#
# Why this exists:
# - Creates only local fixture identities and an OAuth2/OIDC application used by
#   scripts/tests/envoy-ai-gateway-smoke.ps1.
# - The client credentials grant is enabled solely so the smoke can obtain a token
#   with a local fixture app password and without a browser. It is not the product
#   login flow for Codegeist Cloud.
version: 1
metadata:
  name: Codegeist Envoy AI Gateway smoke
  labels:
    blueprints.goauthentik.io/instantiate: "true"
entries:
  - model: authentik_core.user
    identifiers:
      username: codegeist-smoke
    attrs:
      name: Codegeist Smoke User
      email: codegeist-smoke@example.invalid
      is_active: true

  - model: authentik_providers_oauth2.oauth2provider
    id: envoy-ai-gateway-smoke-provider
    identifiers:
      name: envoy-ai-gateway-smoke
    attrs:
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      issuer_mode: global
      client_id: envoy-ai-gateway-smoke-client
      client_secret: codegeist-smoke-envoy-ai-gateway-client-secret
      redirect_uris:
        - matching_mode: strict
          url: http://172.30.198.31:4180/oauth2/callback
          redirect_uri_type: authorization
      grant_types:
        - client_credentials
        - refresh_token
        - authorization_code
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-openid]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-email]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-profile]]
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]

  - model: authentik_core.application
    identifiers:
      slug: envoy-ai-gateway-smoke
    attrs:
      name: Envoy AI Gateway Smoke
      provider: !KeyOf envoy-ai-gateway-smoke-provider
