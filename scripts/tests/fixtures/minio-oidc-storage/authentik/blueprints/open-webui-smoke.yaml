# yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
# open-webui-smoke.yaml - authentik bootstrap for the local Open WebUI smoke UI.
#
# Why this exists:
# - Creates only local fixture identities and an OAuth2/OIDC application used by
#   scripts/tests/envoy-ai-gateway-smoke.ps1 when it starts the optional Open WebUI
#   service for manual browser testing.
# - The fixed user password is test-only and exists so a human can sign in through
#   authentik at the static Docker bridge IP without a mail or recovery flow.
version: 1
metadata:
  name: Codegeist Open WebUI smoke
  labels:
    blueprints.goauthentik.io/instantiate: "true"
entries:
  - model: authentik_core.user
    identifiers:
      username: codegeist-smoke
    attrs:
      name: Codegeist Smoke User
      email: codegeist-smoke@example.invalid
      password: codegeist-smoke-password
      is_active: true

  - model: authentik_providers_oauth2.oauth2provider
    id: open-webui-smoke-provider
    identifiers:
      name: open-webui-smoke
    attrs:
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      issuer_mode: per_provider
      client_id: open-webui-smoke-client
      client_secret: codegeist-smoke-open-webui-client-secret
      redirect_uris:
        - matching_mode: strict
          url: http://172.30.198.40:8080/oauth/oidc/callback
          redirect_uri_type: authorization
      grant_types:
        - refresh_token
        - authorization_code
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-openid]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-email]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-profile]]
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]

  - model: authentik_core.application
    identifiers:
      slug: open-webui-smoke
    attrs:
      name: Open WebUI Smoke
      provider: !KeyOf open-webui-smoke-provider
